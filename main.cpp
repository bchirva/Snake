#include "Game.hpp"
#include "Window.hpp"

int main(int argc, char** argv)
{
    (void)(argc);
    (void)(argv);
    
    Window window;
    window.open();
    //Game app;

    //return app.exec();
    return 0;
}
