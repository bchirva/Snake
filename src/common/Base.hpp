#ifndef BASE_HPP
#define BASE_HPP

#include <stdint.h>
#include <string>

enum class EDirection
{
    Up = 0,
    Down,
    Left,
    Right
};

constexpr uint8_t FIELD_SIZE = 24;

//std::string AppLocation;
#endif
